package songtool

// Note is a single note on a scale.
type Note int

const (
	a      = Note(0)
	aSharp = Note(1)
	bFlat  = Note(1)
	b      = Note(2)
	c      = Note(3)
	cSharp = Note(4)
	dFlat  = Note(5)
	D      = Note(6)
	dSharp = Note(7)
)
